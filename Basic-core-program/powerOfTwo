N = int(input("Enter N: "))

if 0 <= N < 31:
    i = 0
    value = 1
    while i <= N:
        print(f"2^{i} = {value}")
        value *= 2
        i += 1
else:
    print("N must be between 0 and 30")
